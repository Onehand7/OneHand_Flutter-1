name: onehand_app
description: Gestión de servicios y oficios

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image    : ^2.2.0
  cloud_firestore         : ^2.3.0
  cupertino_icons         : ^1.0.2
  curved_navigation_bar   : ^1.0.1
  firebase_core           : ^1.3.0
  firebase_auth           : ^2.0.0
  firebase_storage        : ^9.0.0
  flutter_auth_buttons    : ^0.10.0  
  font_awesome_flutter    : ^9.1.0
  google_sign_in          : ^5.0.4 
  google_fonts            : ^2.1.0
  image_picker            : ^0.8.4+2
  image_cropper           : ^1.4.1
  path_provider           : ^2.0.5
  url_launcher            : ^6.0.9

dev_dependencies:
  flutter_launcher_icons  : ^0.9.2
  flutter_test:
    sdk: flutter
  
flutter_icons: 
  ios: true 
  android: true    
  image_path: "assets/icons/one_hand.png"

flutter:

  uses-material-design: true
  assets:
    - assets/
    - assets/images/
    - assets/images/oficios/
    - assets/images/pay/
